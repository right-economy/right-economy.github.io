# 옳은경제 로드맵 🗺️

> ⚠️ 옛 브랜드명은 **경제한입**이었다. 리브랜딩 후에도 이 제목이 남아 있었다(2026-08-31 정정).
> **`[ ]` 를 「안 됐다」로 읽지 말 것** — 아래 세 항목이 이미 끝났는데 「← 지유」로 남아 있었다.

자동 기능개선 잡(`scripts/auto-feature.sh`)이 이 목록에서 다음 할 일을 고른다.
처리하면 `[x]`로 체크하고, 새 아이디어는 자유롭게 추가.

## 우선순위: SEO · 수익화
- [x] 배포 도메인 (2026-08-31 실측): `astro.config` `site: 'https://right-economy.com'` · `robots.txt` 의 `Sitemap:` 도 같은 도메인 · **canonical 649/649 전부 right-economy.com**
- [x] AdSense 배선 (2026-08-31 실측): `consts.ts` 에 실제 client 값이 들어 있고 렌더된 HTML 에도 나온다. ⚠️ **배선 완료 ≠ 승인 완료** — 승인 상태는 코드로 못 본다
- [x] Search Console (2026-08-31 실측): `GSC_VERIFICATION` 실값 + 렌더 HTML 에 `google-site-verification` 메타태그 존재. ⚠️ **sitemap 제출·색인 상태는 콘솔 쪽이라 코드로 못 본다**
- [x] 글 간 내부 링크 / "관련 글" 섹션 (체류시간·색인↑)
- [x] 카테고리 페이지 (`/category/[name]`)
- [x] 태그 페이지 (`/tags/[tag]`)
- [x] RSS 피드 (`@astrojs/rss`)
- [x] BreadcrumbList JSON-LD (검색결과 경로 표시)
- [ ] 글별 OG 이미지 자동 생성

## 사용자 경험 · 성능
- [ ] 글 목록 검색/필터
- [x] 404 페이지
- [ ] 다크모드
- [ ] 폰트 로딩 최적화 (jsDelivr CDN → self-host 검토) — preconnect는 적용됨
- [x] **접근성 점검 — 완료** (2026-09-06~07 실측). 본문 바로가기·`:focus-visible`·`prefers-reduced-motion`
      + **색 대비 전 조합 AA 통과**(#337) + **aria·시맨틱 태그 실측 이상 0**.
      ```
      aria-label 1,862 · aria-hidden 3,606 · aria-current 725 · role 1,219
      시맨틱(글 페이지)  header·nav·main·article·section·footer·time 전부 존재
      접근 이름 없는 링크  0건 (빈 앵커·라벨 없는 링크 없음)
      ```
      ⚠️ `<aside>`·`<figure>` 0개는 **결함이 아니다** — 본문에 해당 구조가 없어서다. **없는 태그를 넣지 말 것.**

## 완료
- [x] SEO 기반: sitemap · robots.txt · Article/WebSite JSON-LD · AdSense/GSC 배선 (PR #19)
- [x] RSS 피드: `@astrojs/rss` + autodiscovery `<link>` 태그 (PR #?)

## 🔍 SEO 감사 — 열린 이슈: 본문 내부링크가 거의 없다 (2026-09-09 실측)

> ⚠️ **「고아 페이지 0건」과 다른 축이다.** 고아 판정은 목록·태그·관련글의 **자동 링크**까지 세서
> 0/1 을 가르는 것이고, 이건 **사람이 본문에 단 링크의 분포**다. 자동 inbound 는 지금도 전 글 1 이상이다.

```
글 213편 중 다른 글 본문에서 한 번도 안 걸리는 글   168편 (78%)
본문 링크를 내보내는 글                            43편 / 213
본문 inbound 총합                                 100건

카테고리       글수  본문inbound 0   평균 out
시황            21      95%          0.00   ← 하나도 안 내보냄
경제 뉴스        12      91%          0.00   ← 하나도 안 내보냄
부동산          79      88%          0.13   ← 글 수가 제일 많은데 제일 안 걸림
지표 읽기        21      80%          0.71
생활 경제        30      76%          0.53
투자            20      70%          0.60
기초 개념        30      43%          0.33   ← 용어 글끼리만 이어져 있다
```
📌 **시황·경제뉴스는 크론이 매일 만드는 글**이라 여기가 제일 싸게 고쳐진다.
📌 **재료는 이미 있다** — 2026-09-09 시황 글에 CPI 6회·금리 8회·인플레이션이 나오는데
`what-is-cpi`·`what-is-interest-rate`·`what-is-inflation` 글이 전부 존재한다. **억지 링크가 아니라 빠뜨린 자리다.**

⚠️ **자동 치환(remark 플러그인 등)으로 용어를 일괄 링크하지 말 것.** 문맥 없이 걸면 억지 링크가 되고,
같은 용어가 매 글에 반복돼 앵커가 희석된다. **글 쓸 때 자연스러운 자리에만** 거는 게 맞다.

🔴 **이건 코드 이슈가 아니라 발행 절차 이슈다.** 처방은 `PUBLISH_CHECKLIST.md` §3 에 넣었다.

## 🔍 SEO 감사 — 이미 확인된 것 (다시 파지 말 것)

> 이 절은 **SEO 감사 슬롯이 같은 자리를 반복해서 파는 걸 막으려고** 있다.
> 2026-08-31 감사에서 아래 셋을 다시 팠고, **셋 다 이미 답이 있거나 내 측정 도구 문제**였다.

- **태그 페이지가 sitemap 에 330개 없다** → 🟢 **의도된 동작.** 글 2개 미만 태그는 thin content 라 sitemap 에서만 뺀다(페이지 자체는 생성·접근 가능). 2026-08-31 전수 검사에서 **양방향 위반 0** — 1개짜린데 포함된 것 0, 2개 이상인데 제외된 것 0. `S&P`·`S&P500` 이 빠진 것도 `&` 탓이 아니라 **글이 1개**라서다.
- **고아 페이지 2개(`/tags/S&P/` 등)** → 🟢 **오탐.** 링크는 `href="/tags/S&amp;P/"` 로 **올바르게 이스케이프**돼 있고 내 정규식이 `&amp;` 를 못 잡은 것이다. 라이브도 raw·인코딩 둘 다 200.
- **`dist/sitemap-0.xml` 이 없다** → 🟢 **오탐.** 저장소 밖에서 `grep` 을 돌렸다(`cd` 누락). 39KB 로 정상 존재.

- **`<img>` 691개에 alt 가 비어 있다** → 🟢 **의도된 동작이고 고치면 안 된다.** 전부 헤더 로고
  (`/mascot/logo.png`) 하나다. 링크 구조가 `<a class="logo"><img alt=""><span>옳은경제</span></a>` 라
  **옆 텍스트가 이미 링크 이름을 읽어준다.** alt 를 채우면 스크린리더가 "옳은경제 옳은경제"로 중복 낭독한다.
  장식 이미지에 `alt=""` 는 WAI-ARIA 권장 처리다.
- **고아 글(어디서도 링크 안 되는 posts)** → 🟢 **0건** (2026-09-04, dist 191편 전수).
  측정: 모든 dist HTML 에서 `/posts/<slug>/` 링크를 수집해 자기 자신 제외 후 inbound 0 인 글을 셈.
- **h1 이 0개인 페이지 1건** → 🟢 `google9722f43dfe1b8a63.html` (구글 소유확인 파일). 정상.


- **JSON-LD 의 `@type` 이 696개 전부 비어 있다** → 🟢 **오탐.** 최상위가 `@context` + **`@graph` 배열**
  구조라 최상위에 `@type` 이 없는 게 정상이다. `@graph` 를 펼쳐 세면 Article 194 · BreadcrumbList 695 ·
  CollectionPage 498 · ItemList 499 · Organization 196 이고 **Article 필수 필드 누락 0건**
  (headline·datePublished·author·publisher·image·mainEntityOfPage·dateModified 전부 존재).
- **`<title>` 이 65자를 넘는 글 2건** → 🟢 **오탐.** raw HTML 을 세서 `&#39;` (5자) 를 1자로 안 셌다.
  실제 렌더 길이는 73→57 · 67→51 로 **둘 다 60자 이내**다. 📌 **문자 수를 셀 땐 엔티티를 먼저 푼다.**
- **sitemap 과 실제 페이지 대조** → 🟢 **완전 일치** (2026-09-05, dist 699 HTML).
  `sitemap URL 362 = robots meta 가 index 인 페이지 362`, **sitemap 에 실린 noindex 0 · index 인데 누락 0.**
  ⚠️ 첫 측정에서 **양쪽이 똑같이 154** 로 나왔는데 그게 일치가 아니라 **고장**이었다 — sitemap 은
  퍼센트 인코딩(`%EA%B2%BD…`)이고 파일명은 한글이라 `unquote` 없이 대조했다. **A/B 가 완벽히 같으면
  일치가 아니라 도구 고장을 먼저 의심한다.**

- **커스텀 도메인 전환이 색인 경로에 반영됐나** → 🟢 **전부 반영됨** (2026-09-08 라이브 실측).
  GitHub Pages 시절 GSC `couldn't fetch` 의 처방이 커스텀 도메인이었고, 그 전환이 실제로 끝났는지 본 것.
  ```
  astro.config site        https://right-economy.com
  robots.txt (로컬·라이브)  Sitemap: https://right-economy.com/sitemap-index.xml
  sitemap-0.xml            <loc> 384개 · 전부 right-economy.com · 옛 도메인 0
  당일 발행 글             sitemap 에 즉시 포함됨
  글 canonical             https://right-economy.com/posts/<slug>/ (절대 URL)
  ```
  ⚠️ **GSC 색인 상태 자체는 봇이 못 본다.** 여기서 확인한 건 「우리가 내보내는 입구」까지다.
  📌 개수는 `grep -o "<loc>" | wc -l` 로 셌다 — sitemap 은 minify 된 한 줄이라 `grep -c` 는 무조건 1이다.

- **터치 대상 크기(WCAG 2.5.8, 최소 24×24)** → 🔧 **실질 이슈 1건 수정** (2026-09-08, 390×844 뷰포트).
  ```
  홈 221개 · 글상세 18 · 카테고리 19 · 태그 8   → 미달 17개
  원인: .footer-nav a 에 패딩이 없어 22px (전 페이지 반복 · 개인정보·면책 링크)
  조치: 세로 패딩 4px + gap 8→4  → 30px  · 재측정 17 → 1
  ```
  🔴 **처음 측정은 「41개 중 41개 미달」이 나왔는데 전부 오탐이었다.**
  `file://` 로 dist 를 열어서 **CSS 가 하나도 안 붙은 상태**를 잰 것이다
  (`/_astro/*.css` 절대경로가 `file:///_astro/...` 로 나가 404). 값이 **전부 정확히 19px** 로 같았던 게 신호였다.
  📌 **A/B 가 완벽히 같으면 일치가 아니라 도구 고장을 먼저 의심한다** — 이 저장소에서 세 번째다.
  🔧 **확인 방법: `padding` 이 `0px` 이거나 `body` 배경색이 투명이면 CSS 가 안 붙은 것이다.**
  dist 측정은 `python3 -m http.server` 로 **서빙해서** 해야 한다(base 가 `/` 라 file:// 로는 안 됨).
- [ ] 🟡 **글 상세의 `.category` 칩이 59×19 (24px 미달) — 리뷰行**
  `.card .category` 규칙이 카드 밖이라 안 걸려서 **스타일이 거의 안 붙은 상태**다.
  패딩만 주면 다른 칩(`tag-chip` 55×29)과 모양이 달라지고, **디자인 누락인지 의도인지 판단이 필요**하다.

📌 **2026-09-05 감사 결론: 실질 이슈 0건.** 메타태그(중복 title 0 · 중복 description 0 · 누락 1건은
구글 소유확인 파일) · canonical(698/699, 상대경로 0) · OG·트위터 이미지(698) · JSON-LD · sitemap 정합
전부 통과. **이 슬롯에서 뜬 🔴 3건이 전부 측정 도구 문제였다** — 세 번째 반복이다.

- **heading 계층** → 🟢 **건너뛴 페이지 0건** (2026-09-05, dist 706 전수). `h2→h4` 같은 도약 없음.
- **신규 글이 고아로 시작하나** → 🟢 **아니다.** 발행 당일 글 3편의 inbound가 각 **12·15·23**
  (다른 글·태그·카테고리·홈에서 자동으로 걸린다). posts 196편 전체 고아 **0건**.
  📌 신규 글은 유입이 늦어 고아처럼 보이기 쉬운데, **링크 구조상 발행 즉시 붙는다.**
- 🔴 **`dist/cards/` 32MB 를 「이미지 최적화 대상」으로 착각하지 말 것** (2026-09-05 실측).
  ```
  cards 아래 HTML        0개
  sitemap 포함           0개
  글 본문에서 임베드      0개   ← SEO/LCP 경로에 아예 없음
  ```
  인스타 발행용 호스팅 자산이다([[project_econ_instagram_publish]]). **dist 40MB 중 32MB가 여기라
  「사이트가 무겁다」로 읽히지만 방문자는 한 장도 안 받는다.** 본문 이미지는 글당 png 1(헤더 로고)
  + svg 155(본문 도표)뿐이다. 측정: `dist/posts/**` 에서 `src="/cards/…"` 수집 → 0건.

- **본문 이미지 무게·우선순위** (2026-09-06 실측, dist 713). 임베드 이미지 159종 중 **150KB 초과는 1개**뿐.
  🔧 그 1개(`benner-original-1884.jpg` 536KB)가 **문서 52% 지점(스크롤 아래)인데 `loading="eager"
  fetchpriority="high"`** 였다 — 반 메가짜리 아래쪽 이미지를 최우선으로 받으라고 브라우저에 지시하던 것.
  `loading="lazy"` 로 바꾸고 `fetchpriority` 를 뺐다(PR #334). **사이트 전체 fetchpriority=high 는 이제 0.**
  ⚠️ 본문 이미지 `loading` 은 **eager 94 / lazy 64** 로 갈려 있다. 이건 글마다 저자가 손으로 적은 값이라
  일괄 변경하지 않았다 — **위쪽 이미지엔 eager 가 맞다.** 고칠 거면 글별로 위치를 보고 판단할 것.
- **앵커 텍스트** → 🟢 문제 없음. ⚠️ **내 첫 측정이 오탐이었다** — `len(텍스트) < 3` 으로 걸렀더니
  `금리`·`주식`·`연금` 같은 **한국어 두 글자 단어**가 「빈약한 앵커」로 잡혔다. 한국어에서 두 글자는
  충분히 구체적이다. **영어 기준 필터(`여기`·`클릭`·`더보기`)만 남기면 0건.**
- **헤더 로고에 `loading` 이 없다(199건)** → 🟢 **정상.** 상단 고정 로고라 지연 로딩 대상이 아니다.
  `width`/`height` 가 박혀 있어 레이아웃 시프트도 없다. **본문 이미지 중 `loading` 누락은 0건.**

- **색 대비(WCAG AA)** → 🟢 **전 조합 통과** (2026-09-06 실측). 팔레트가 애초에 안전하게 잡혀 있다.
  ```
  ink   /bg 14.55 · muted /bg 5.40 · accent /bg 4.97   (기준 4.5)
  ```
  `--muted` 는 `.card-meta`·`.breadcrumb`·`figcaption` 등 **0.78rem 작은 글씨**에 쓰이는데도 5.40 이고,
  `--accent` 는 본문 링크(`a`) 색인데 4.97 이다. **이 축은 더 팔 게 없다.**
  ⚠️ **자매 프로젝트 우쿨코드는 같은 축에서 미달(3.65)이 나와 같은 날 고쳤다**(ukucode #20).
  **한쪽이 통과했다고 다른 쪽도 통과라고 보지 말 것** — 팔레트가 다르다.
- **접근성 기본** → 🟢 (2026-09-06). `lang="ko"` · skip link · `:focus-visible` · `prefers-reduced-motion` ·
  `<main>` 1개 · `theme-color` 전부 있음. **h1 은 722/723 이 정확히 1개**(0개인 1건은 구글 소유확인 파일).

- **RSS 피드** → 🟢 정상 (2026-09-07). `rss.xml` item **203개 = 글 수와 일치** · 109KB ·
  최신순 정렬 · autodiscovery `<link rel="alternate">` 있음 · title·link·description·pubDate·guid·category 전부.
  📌 **전문(`content:encoded`)은 안 싣는다** — description 평균 101자만. 중복 콘텐츠 위험이 없으니 **전문을 넣지 말 것.**
- **외부 링크 안전성** → 🟢 **위반 0건** (2026-09-07). `target="_blank"` 인데 `noopener` 없는 링크 0.
  외부 호스트도 인스타그램·구글 광고설정 **2개뿐**이라 표면이 애초에 좁다.

- 🔴 **sitemap `lastmod` 가 375/379 인데 「4개 누락」이 아니다 — 채우면 안 된다** (2026-09-07).
  빠진 넷은 `/about/ /contact/ /disclaimer/ /privacy/` **정적 페이지**다. 글이 아니라 **콘텐츠 날짜가 없어서**
  Astro sitemap 이 `lastmod` 를 안 붙인다. `lastmod` 는 sitemap 규격상 **선택 항목**이고,
  **날짜를 지어 넣으면 「최근 갱신됨」이라는 거짓 신호**를 검색엔진에 보낸다.
  → **비어 있는 게 정확한 상태다.** 숫자만 보면 결함처럼 보이니 근거와 함께 닫는다.
- **sitemap 정합·URL 표기·breadcrumb** → 🟢 전부 통과 (2026-09-07, dist 735).
  `sitemap 379 = index 대상 379` · trailing slash **379/379 일관** · canonical 불일치 0 ·
  **BreadcrumbList 마지막 항목 ≠ canonical 인 글 0건**.

📌 **SEO 감사에서 🔴 이 뜨면 대상보다 내 측정 도구를 먼저 의심한다.** 2026-08-29 감사에서도 622페이지 실측 🔴 2건이 **둘 다 파서 오탐**이었다. 같은 패턴이 두 번 반복됐다.
