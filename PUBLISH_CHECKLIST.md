# 옳은경제 발행 체크리스트

복덕·꼬미·키리가 **같은 기준**을 보기 위한 문서. 작성자는 쓰기 전에, 검수자(키리)는 승인 전에 이 순서로 본다.

> **왜 만들었나 (2026-08-30)**
> 발행 규칙이 각 봇 메모리에 흩어져 있어서, **작성자가 잊으면 검수자도 못 잡았다.**
> 그날 복덕 글이 "대표님 DM에서 소재를 가져온 시의성 없는 글"로 나갔다가 대표님 지적으로 내려갔다.
> 복덕 메모리엔 8/4자로 그 규칙이 있었지만 **키리 검수 항목엔 없었다.**
> 작성자가 검수 요청에 소재 출처를 *자랑처럼* 적었다는 게 핵심 신호다 — **숨긴 게 아니라 문제인 줄 몰랐다.**
> 그러면 개인 규율로는 안 잡힌다. 그래서 목록으로 뺀다.

---

## 0. 소재 — 여기서 막히면 아래는 볼 필요 없다

- [ ] **최근 뉴스에서 왔는가.** 웬만하면 "요즘 뜬 이슈" 기반 시의성 글. 실거래·통계는 **소재가 아니라 차별화 재료**다(뉴스에 붙이는 것).
- [ ] **대표님 개인 대화·개인 자산에서 가져오지 않았는가.** DM 대화, 대표님이 보시는 매물·지역은 소재 금지. 대중과 접점이 없다.
- [ ] **사적 연관 지역이 아닌가.** 의왕·산본 등 대표님이 개인적으로 언급하는 지역은 소재 단계에서 반려.
- [ ] **독자가 검색할 주제인가.** "내가 설명하고 싶은 것"과 "사람들이 찾는 것"은 다르다.
- [ ] 🔴 **첫 줄이 독자의 상황인가, 내 설명 순서인가.** 검수를 통과하는 구성과 읽고 싶은 구성은 다르다.
      **정확하게 쓰려고 하면 정의부터 늘어놓게 된다** — 용어 → 개념 → 그래서 당신 얘기 순서가 되고,
      독자는 첫 줄에서 자기 얘기를 못 찾는다. **경험을 앞에, 용어를 뒤에** 놓는다.
      ```
      ❌ 천간이란 겉으로 드러나는 나예요        ← 맞는 말인데 남 얘기로 읽힘
      ✅ 머릿속엔 다 있는데 몸이 안 따라간다     ← 자기 얘기로 읽힘. 설명은 뒤에 붙임
      ```
      > 2026-09-05 대표님 지적 두 건이 같은 축이었다 — 나랏빚 글 **"아무도 안 궁금해해"**,
      > 우주사주 카드 **"공감이 안 돼"**. 둘 다 사실은 맞았고 **독자와의 거리**가 문제였다.
      ⚠️ **검수 게이트가 이 방향으로 잡아당긴다.** 검수자에게 통과받을 구성으로 짜면 용어 중심이 된다
      (우주 자기진단: *"검수를 통과하는 데만 신경 쓰다가 읽는 사람을 놓쳤다"*).
      **게이트는 「틀리지 않았나」만 재고 「읽고 싶은가」는 아무도 안 잰다** — 그래서 여기 항목으로 둔다.
      🔴 **단, 용어를 없애는 게 처방이 아니다.** 다 걷어내면 **"이게 무슨 분야 얘기인지"가 사라지고**
      전문성·신뢰·다음 클릭 이유가 같이 빠진다. **문제는 용어가 아니라 용어로 시작하는 것**이다.
      → **경험 문장 뒤에 괄호로 한 번** 붙이면 둘 다 산다. `남의 겉(천간)만 보고 내 속(지지)이랑 비교해서`
      → 깊은 설명은 본문·캡션으로 내린다. **제목·첫 줄은 훅, 뒤는 학습.**
      (2026-09-05 우주사주 카드: 경험 중심으로 뒤집자 명리 용어가 0회가 됐고 만세가 되돌림)
- [ ] 🔴 **결론이 「못 정한다·모른다·시차가 길다」로 끝나지 않는가.** 그러면 그 소재는 내 것이 아니다.
      ⚠️ **「잴 수 없는 걸 정직하게 적는 것」과 「애초에 잴 수 없는 걸 주제로 삼는 것」은 다르다.**
      앞은 미덕이고 뒤는 소재 선정 실패다. **본문 안의 한 문단이면 정직한 것이고, 그게 결론이면 글이 설 자리가 없다.**
      ```
      ⭕ 취소거래 글    "빠진 표본이 비싼 쪽에 쏠렸는지는 못 쟀다"   ← 본문 한 대목
      ❌ 환율↔부동산   "환율로는 집값 방향을 못 정한다"            ← 이게 결론
      ```
      > 2026-09-07 복덕이 한 번 뺀 소재를 **키리가 "각도를 바꾸면 된다"며 되살렸다.**
      > 「크기를 모른다」를 04장에 그대로 넣자고 한 것이 바로 그 자리였다. 대표님 **"환율이랑 부동산 엮지마"**.
      🔴 **한 번 뺀 소재를 다시 여는 건 「새 근거」가 있을 때만.** 「새 각도」는 근거가 아니다.
- [ ] 🔴 **이 소재가 「내 분야」로 와야 할 이유가 있는가.** 없으면 각도를 찾지 말고 **그 소재 그대로** 쓴다.
      ⚠️ 옳은경제는 **종합 경제 계정**이다. 부동산 계정도, 시황 계정도 아니다.
      그런데 소재가 오면 **받은 봇이 자기 분야로 끌어온다** — 그 순간 억지 연결이 생긴다.
      ```
      환율 뉴스 도착 → 부동산 담당이 받음 → "집값과 어떻게 엮지" → 인과가 약한 글
      환율 뉴스 도착 → 그냥 환율 글                              ⭕ 이게 맞다
      ```
      > 2026-09-07 대표님 **"우리 부동산 계정이 아닌디 왜 환율까지 부동산이랑 엮어"**.
      > 🔴 봇 셋이 「인과가 약하다」는 옆 문제를 붙들고 반성하는 동안 **아무도 계정 성격을 안 물었다.**
      📌 **담당 분야는 소재를 배정받는 기준이지, 소재를 변형할 권한이 아니다.**
- [ ] 🔴 **몇 개를 보고 몇 개를 말하는가 — 근거 범위를 문장에 적는다.**
      값이 맞는지와 **그 값이 몇 개를 근거로 하는지**는 다른 축이다. 검산은 값만 보고 범위는 안 본다.
      ```
      본 것    상위 3곳
      쓴 문장  "다섯 단지 … 그 단지들은 개인 매수가 0건"
      결과     실제로 다섯 곳 다 0건이라 사실은 맞았다 — 그런데 그때는 몰랐다
      ```
      > 2026-09-08 복덕 자기진단. **"맞은 건 운이고, 그때 저는 몰랐습니다."**
      🔴 **결과가 맞으면 이 오류는 영원히 안 걸린다.** "확인해보니 맞았습니다"로 끝내면 다음에도 셋을 보고 다섯을 말한다.
      🔧 **문장에 근거 범위를 같이 적는다** — `다섯 곳 모두 확인` / `상위 3곳 기준` 처럼.
         전수를 못 봤으면 **문장을 본 만큼으로 줄인다.**
      📌 [[문장이 데이터 해상도를 넘는다]] 와 같은 뿌리인데 **축이 다르다** — 그건 값이 넘은 것이고
         이건 **표본 수가 주장 범위를 못 받친 것**이라, 값 검산으로는 안 잡힌다.
- [ ] **기존 168편과 안 겹치는가.** 쓰기 전에 `src/content/posts/` 제목 전수 확인.

> 📌 검수 요청에 **뉴스 링크와 발행 시각**을 먼저 적는다. 없으면 검수자는 수치를 안 보고 반려한다.

---

### 가드를 어디에 두나 (2026-09-08 우주사주 릴스에서 나옴 — 카드·글 공통)

- [ ] 🔧 **가드는 뒤에만 붙이지 말고 「앞에서 프레임을 잡는」 방법을 먼저 본다.**
      뒤에 붙는 가드는 **안 읽고 넘기면 끝**이고, 힘을 빼는 문장이 되기 쉽다.
      앞에 깔면 **오독이 시작을 못 한다.**
      ```
      소재 설명   "합(合) — 혼자서는 안 나오고 상대가 있어야 나오는 자리"
      효과        「내 사주만으로 상대를 판정」하는 오독을 첫 줄에서 차단
      ```
      📌 우주 자기관찰 — **"가드로 쓴 게 아니라 소재를 설명한 문장인데 가드가 되고 있었다."**
      → 설명 문장을 쓸 때 **"이 문장이 무슨 오독을 미리 막나"** 를 한 번 본다.
- [ ] 🔴 **줄일 때 「무엇이 빠졌는지」를 센다 — 빠진 게 가드일 수 있다.**
      길이를 맞추려 줄이면 **반드시 뭔가 빠지는데, 보통 「짧게 고쳤다」로 끝난다.**
      ```
      실제   릴스 5번 줄이 두 줄로 넘쳐 짧게 고침
             → 「궁합은 한쪽 사주만으론 안 나온다」 가드가 카드에서 빠지고 캡션에만 남음
      조치   작성자가 스스로 세어보고 "이게 카드에 꼭 있어야 하나"를 물음
      ```
      🔧 **줄인 뒤 원문과 나란히 놓고 사라진 문장을 나열한다.** 그중 가드가 있으면 다른 자리를 찾는다.
      📌 [[요약 장이 조건을 떨군다]] 와 같은 뿌리 — 그건 요약이 떨구는 것이고 이건 **분량 맞추기가 떨구는 것**이다.

---

## 1. 사실 — 숫자와 인용

- [ ] **모든 수치에 기준 시각이 붙었는가.** 이벤트 전후로 갈리는 값은 `○○ 전 A% → 후 B%`로 **둘 다** 적는다.
- [ ] **집계 기관·매체가 갈리면 범위 + 각 값 병기.** 뭉개지 않는다.
- [ ] **출처는 내가 실제로 본 경로로.** CME를 직접 조회 안 했으면 `CME FedWatch 인용 보도`라고 적는다.
- [ ] 🔴 **시행일·개정일·발표일은 검색 요약으로 정하지 않는다 — 1차 출처만.**
      법령·조례·제도의 날짜는 **개정이 여러 번 쌓여 스니펫마다 다른 회차를 집는다.** 요율·금액 같은
      본문 값은 같은데 **연도만 다르게** 나오므로, 값이 맞는 걸 보고 날짜까지 맞다고 넘어가기 쉽다.
      🔴 **원인은 「스니펫이 부정확해서」가 아니라 「법령의 층위가 여럿」이라서다.**
      ```
      공인중개사법 시행규칙(국토교통부령)   상한요율의 큰 틀        2021.10.19 시행
      서울특별시 조례 별표1                 그 범위 안에서 시·도가   2021.12.30 시행
      ```
      부령이 먼저 바뀌고 조례가 따라 바뀌므로 **같은 제도에 시행일이 여러 개**다. 스니펫은
      **어느 층위를 집었는지 안 알려준다.** 그걸 모른 채 날짜만 옮기면 갈린다.
      → 글이 조례 요율표를 싣는다면 **조례 시행일**을, 부령을 말한다면 **부령 시행일**을 적는다.
      > 실사고 2026-09-05 중개보수 글: 검색 요약이 `2024.10.19` · `2021.10.19` · `2022.12.30 제8585호`로
      > 갈렸고, 이 글이 인용한 조례 별표1의 정답은 **2021.12.30**(서울시 부동산정보광장 원문)이었다.
      > ⚠️ **작성자가 1차 출처로 맞게 적은 것을 검수자가 스니펫으로 「틀렸다」고 뒤집을 뻔했다** —
      > 검증한다며 더 약한 근거를 가져온 꼴이다.
      → 날짜를 뒤집으려면 **뒤집는 쪽이 1차 출처**여야 한다. 스니펫끼리 다투게 두지 않는다.
- [ ] **내부 링크로 우리 글을 인용할 땐 그 파일을 열어본다.** 기억으로 인용하지 않는다. 인용 후 `grep`으로 수치 잔존 확인.
- [ ] 🔴 **인용하지 않아도, 같은 사실을 다루면 우리 과거 글과 대조한다 — 우리 사이트 글도 1차 출처다.**
      밖으로 맞는지만 보고 **안으로 맞는지를 안 보면**, 두 글이 나란히 서서 독자가 어느 쪽이 우리 입장인지 못 고른다.
      ```
      확인 대상   외부 1차 출처  +  같은 사실을 다룬 우리 과거 글
      찾는 법     src/content/posts/ 를 그 사실의 키워드로 grep → 원문 열기
      ```
      > 실사고 2026-09-07 환율 글: 초안이 하락 원인을 **「한은 금리 인상 기조」**(검색 요약)로 잡았는데,
      > 우리 9/5 시황은 **「미국 금리 동결 기대 + 경상수지 흑자」**로 이미 써놨다. 한은은 안 나온다.
      📌 **원인이 여럿이면 여럿이라고 쓴다.** 하나로 좁히면 그 하나가 틀렸을 때 논지가 통째로 무너진다.
      ⚠️ **연재물은 어제 자기가 한 말이 오늘의 검수 기준**이다. 같은 축이 이틀에 네 번 났다 —
      시황 분모 이동 · 우주사주 시리즈 2건 · 이 건.
- [ ] **인용할 때 수치만 보지 말고 "그 글이 무엇을 쟁점이라 했는지"까지 본다.** 같은 사안이라도 변수가 다르면 근거가 안 된다. 2026-08-30: 광명 글의 쟁점은 임대 **비율**(얼마나 넣나)인데 재개발 글이 임대 **인수가격**(얼마에 사주나)의 근거로 끌어왔다. 둘 다 분담금에 닿지만 같은 것이 아니다.
- [ ] **원글이 "이 둘을 비교하지 말라"고 적어뒀는지 확인.** 재는 방법이 다른 수치를 나란히 놓고 결론내지 않는다.
- [ ] **경계값은 집계 직후 검산.** `>10` 과 `>=10` 이 몇 건 갈리는지 세고, 표 캡션에 기준을 명시한다.
- [ ] **비교·대조엔 예외 카운터를 같이 출력.** `차이 0건`과 `측정 실패`는 화면에 똑같이 찍힌다.
- [ ] 🔴 **지수·유가·환율은 검색 인용 금지 — 직접 조회한다.** 보도는 값을 옮겨 적다 틀리고, 특히
      **다른 유종·다른 종목의 값을 섞는다.** 종가·등락률·고가는 API로 직접 받는다.
      ```
      ~/kkomi/.venv/bin/python -c "
      import yfinance as yf
      h=yf.Ticker('BZ=F').history(start='YYYY-MM-DD', end='YYYY-MM-DD')
      print(h[['Open','High','Low','Close']])"
      # ^GSPC S&P500 · ^IXIC 나스닥 · ^DJI 다우 · CL=F WTI · BZ=F 브렌트
      ```
      📌 위 파이썬 경로는 **남의 프로젝트 venv라 언젠가 사라진다.** 안 되면 이렇게 찾는다 —
      `find ~ -maxdepth 6 -name yfinance -type d -not -path "*/node_modules/*"`
      ⚠️ **조회 결과에 구멍이 있는지 먼저 본다.** 날짜를 명시해 받고 **행 수를 센다** — 휴장이 아닌데
      빠진 날이 있으면 그 값으로 등락률을 계산하는 순간 **하루치가 이틀치가 된다.**
      🔴 **빠질 땐 하루가 통째로, 전 종목에서 빠진다.** 2026-08-28이 `^GSPC`·`^IXIC`·`^DJI`·`SPY`·
      `^VIX`·`AAPL` 에서 **전부** 없었다. 한 종목만 비면 이상하다고 느끼는데, **다 비면 "그날 휴장이었나"
      하고 넘어간다.** 실제로 8/28은 **정상 거래일**이었다(2026년 8월엔 미국 증시 휴일이 없다).
      → **판별: ⓪이름을 틀린 건 아닌가(다음 항목) ①다른 종목에도 없나 ②휴장일 캘린더에 있나
      ③구간을 바꿔 다시 받아도 없나.** ③은 **"내가 구간을 좁게 잡아서 그런가"** 를 지우는 단계다 —
      8/28은 `start/end` 를 넓혀도, `period='1mo'` 로 받아도 안 나왔다(**소스 공백 확정**).

- [ ] 🔴 **`0건`·`404`·`빈 값`을 봤으면 「대상이 없다」고 적기 전에 「내가 이름을 틀렸다」를 먼저 지운다.**
      **제일 흔하고 제일 싸게 확인되므로 맨 먼저 한다.** 키·파일명·필드명·URL을 **소스에서 직접
      출력해 대조한다** — `print(list(d.keys()))` · `ls` · **이미 되는 걸로 한 번 찍어보기.**
      > 실사고 2026-09-01 하루에 넷 (복덕 2 · 키리 2)
      > • 매물 JSON을 `a['articles']` 로 파싱 → 0건 → "매물 없음" 쓸 뻔. 실제 키는 `sampleArticles`, **25건 있었다.**
      > • 카드 이미지를 `1.png` 로 조회 → 404 → "배포 실패" 올릴 뻔. 실제는 `iju-01-cover.png`.
      > • 발행 글 라이브가 404 → **어제 글로 먼저 찍어** 주소는 맞고 배포가 안 끝난 것으로 갈렸다.
      > • `모름/해외` 로 검색 0건 → 실제 표기는 `모름 / 해외`(공백 포함). ※ 우주사주 결과지 필드
      ⚠️ **넷 다 대상은 멀쩡했고 이름이 틀렸다. 그리고 조용히 통과한다** —
      파이썬 `dict.get()` 은 없는 키에 에러 대신 `None` 을 주고, `grep` 은 표기가 다르면 0을 준다.
      **`0`과 `조회 실패`는 화면에 똑같이 찍힌다.**

- [ ] 🔴 **"이게 내가 쓰려는 그 숫자인가" — 같은 이름의 값이 여러 개인 자리를 먼저 의심한다.**
      집값은 **호가 / 실거래(신고 30일 지연) / 감정평가액 / 공시가격**이 전부 다르고, 환율은
      **서울 주간거래 종가 / 24시간 역외 호가**가 다르다. 이름이 같다고 같은 값이 아니다.
      → **비교·인용 전에 "무엇을 재는 값인지"를 적고, 다른 종류면 갈라서 적는다.** (복덕)

- [ ] 🔴 **여러 티커를 나란히 놓기 전에 `h.index.tz` 를 찍는다 — 티커마다 기준 시각이 다르다.**
      ```
      KRW=X → Europe/London   |   ^GSPC·CL=F → America/New_York   |   ^KS11 → Asia/Seoul
      ```
      **행 라벨의 날짜가 서로 다른 기준**이라, 그대로 "같은 날"로 읽으면 어긋난다. `KRW=X` 가 하루
      밀려 보이는 것도 이 때문이다(런던 기준이라 서울과 8~9시간 차).
      ⚠️ **장중에 받은 값은 종가가 아니다.** 같은 행이 몇 분 뒤 다른 값이 된다
      (2026-09-01 `KRW=X` 당일 행: 1,368.20 → 1,367.26). **"종가"로 인용하려면 그 시장이 닫힌 뒤에 받는다.**

- [ ] 🔴 **국내 지수·환율은 위 API로 받지 않는다 — 국내 마감 보도를 쓴다.**
      ```
      직접 조회 ⭕   미국 지수(^GSPC·^IXIC·^DJI) · 유가(CL=F·BZ=F)
      직접 조회 ❌   코스피·코스닥(^KS11·^KQ11)  → 결측이 상시. 최근 거래일이 통째로 없다
                    원/달러(KRW=X)              → 서울 주간거래 종가와 **다른 지표**
      ```
      - **`^KS11`·`^KQ11`은 최근 며칠이 통째로 빈다.** 2026-09-01 조회 시 8/28·8/31이 없어
        8/27이 최신으로 보였다. 모르고 쓰면 **이틀 전 값을 어제 종가로 싣는다.**
      - **`KRW=X`는 24시간 호가 기준이라 서울 주간거래 종가와 약 8원 벌어진다**
        (2026-08-31: KRW=X 1,377.11 / 보도 1,368.6). 게다가 **행 날짜가 하루 밀려 붙는다** —
        KRW=X의 `9/1` 행(1,368.20)이 국내 `8/31` 종가에 가깝다. 섞으면 **같은 날 환율이 두 개**가
        되고 과거 글의 흐름과도 어긋난다.
      > 📌 **"직접 조회가 항상 낫다"가 아니다.** 재는 대상이 다르면 직접 조회가 오히려 틀린 값을 준다.
      > 지표를 고를 땐 **"이게 내가 쓰려는 그 숫자인가"** 를 먼저 본다. (꼬미 발견, 2026-09-01)
- [ ] 🔴 **소스가 갈리는 걸 발견했으면 「적고 넘어가기」 금지 — 열어서 어느 쪽이 맞는지 정한다.**
      "보도마다 다르니 방향과 폭으로 보세요" 같은 회피 문구는 **검증한 게 아니라 덮은 것**이고,
      **덮은 자리가 대개 틀린 자리다.** 갈린 걸 못 풀겠으면 그 문장을 빼거나 검수자에게 넘긴다.
      > 실사고 2026-09-01: WTI 절대가가 갈리는 걸 작성자가 **발견해서 본문에 적기까지 했는데**
      > 풀지 않고 넘어갔다. 정작 틀린 건 옆에 있던 **브렌트유였고 방향이 반대**였다(90 돌파 →
      > 실제 −0.76%). 갈린 걸 열었으면 같이 걸렸다.
      > ⚠️ **틀린 축은 글을 오히려 매끄럽게 만든다** — 90 돌파를 전제하니 뒤 문단이 술술 써졌다.
      > **잘 읽힌다는 건 맞다는 증거가 아니다.**

---

## 2. 표현 — 강도와 방향

- [ ] **원 보도보다 세게 쓰지 않았는가.** 언론이 의문형("멈추나")이면 우리도 단정하지 않는다.
- [ ] **약하게 쓰지도 않았는가.** 중립은 "양쪽 같은 무게"가 아니라 **사실을 끝까지 쓰는 것**이다.
- [ ] **단정 + 괄호 완화는 작동하지 않는다.** 본문 어미 자체를 낮춘다(`~한 편이에요` / `~쪽에 가까워요`). 완화 문구가 세 번 나오면 본문이 규정 중이라는 신호.
- [ ] **당사자가 반박할 수 있는 자리인가.** 확정 안 된 걸 확정으로 쓰면 상대가 공식 반박할 수 있고 그때 우리가 틀린 게 된다.
- [ ] **특정 단지·지역·인물을 지목했는가.** 극단 사례는 지역구+면적까지만, 단지명 제외, 원인은 *가능성*으로만.
- [ ] **정치중립.** 정당·정치인 실명 편들기/저격 X. 시장친화 경제관은 은은하게만.
- [ ] **투자 권유 없음.** 특정 종목·단지 추천 X.
- [ ] **면책 문구 포함.** 정보 제공 목적 · 투자 권유 아님 · 데이터 변동 가능.
- [ ] 🔴 **읽는 시점에 따라 뜻이 변하는 표현을 쓰지 않았는가 — 절대 날짜로 적는다.**
      ```
      금지   이번 주 · 다음 주 · 오늘 · 어제 · 사흘째 · 최근
      대신   9월 둘째 주(9/7~) · 9월 4일(금) · 8/31~9/3
      ```
      🔴 **금지는 「요일 단독」이다.** `수요일 반등`처럼 날짜 없이 요일만 있으면 한 칸 밀려도 안 걸리고,
      표에 들어가면 옆 열까지 같이 민다. 반대로 `9/3(목)`은 **둘이 서로를 검산**해서 어긋나면 그 자리에서 보인다.
      ```
      금지   요일 단독      수요일 반등 · 목요일 하락
      권장   날짜에 병기    9/3(목) · 9월 7일(월)
      ```
      📌 **청약·계약일처럼 요일이 실용 정보인 자리도 있다**(`9/7(월) 특별공급`). 요일을 지우면 독자가 다시 찾아본다.
      지울 대상은 요일이 아니라 **기준점 없는 상대 표현**이다.
      ⚠️ **캡션엔 맞게 쓰고 본문에서 어긋나는 게 전형**이다. 캡션 `9월 둘째 주 분양 예정` ↔ 본문
      `이번 주 2,852가구 분양을 시작했는데` — 같은 글 안에서 시제가 갈렸고, 아직 시작도 안 한 분양이었다.
      🔴 **표지·요약에 실리면 정정이 안 된다.** 본문은 고치면 되지만 카드 표지와 이미 나간 브리핑은 남는다.
      > 실사고 2026-09-05 하루에 셋 — 우주사주 스레드(`이번 주 일요일`→9/7은 다음 주 월요일) ·
      > 시황 PR #318(미국 요일 표가 하루씩 밀려 4행 중 3행 오류) · 부동산 2,852 글(시제).
      > **셋 다 숫자는 맞았고 시점 표현만 틀렸다.** 숫자를 검산하는 절차는 있는데 시점은 아무도 안 쟀다.
      → **원고를 쓰기 전에 발행일 기준으로 날짜·요일을 한 번 찍고 시작한다.** 다 쓴 뒤엔 표까지 얽혀 비싸다.

- [ ] 🔴 **검수 지적을 반영해 본문을 고쳤으면, 그 자리를 요약에서 다시 찾는다.** 아래 항목은 「처음 쓸 때」용이고
      **계속 새는 건 「고친 뒤」다.** 본문만 고치고 요약은 옛 주장을 그대로 들고 있는다.
      → 고친 **문장이 아니라 값·주장**으로 grep 한다. (`시차` `절반` 처럼 표현이 바뀌었을 수 있다)
      > 2026-09-05·09-06 시황 글에서 **사흘 연속.** 본문 "원인은 시차"를 "방향은 시차·폭은 수급"으로
      > 고쳤는데 정리 섹션엔 "원인은 하루 시차"가 남아 있었다(꼬미 자체 발견).
- [ ] **요약·마지막 장을 본문과 대조했는가 — 조건 유무 + 강도 두 축으로.** 본문에서 낮춘 문장이 요약에서 단정형으로 되살아난다. 조건(`예정`·`한시`·`시행 전`)이 살아있는지만 보지 말고, **주장이 본문보다 세지지 않았는지**도 본다.

> 📌 **제일 자신 없는 주장이 요약에서 제일 세지는 게 흔한 패턴이다.**
> 본문에선 근거가 얕아 조심해서 쓰고, 요약에선 줄이느라 단정한다. 2026-08-30 하루에 세 번 났다 —
> 옳은경제 금리차 글 요약, 하안주공 카드 06장, 재개발 글 핵심 정리.
> **작성자가 "여기 자신 없다"고 적은 항목부터 요약에서 찾아본다.** (복덕 제안)

---

## 3. 형식

### 본문에 다른 글 링크 1~2개 (2026-09-09 감사에서 나옴)

**있으면 걸고, 없으면 안 건다.** 개수를 채우려고 억지로 넣지 않는다.

```
걸 자리   본문에 나온 용어 중 우리에게 설명 글이 있는 것
          예) CPI·금리·환율·인플레이션 → what-is-cpi / what-is-interest-rate / …
          같은 사건을 다룬 우리 이전 글
안 걸 자리 문맥이 안 맞는데 용어만 같은 것 · 같은 글에 같은 대상 두 번
```
⚠️ **관련글 위젯이 있어도 본문 링크는 따로 필요하다.** 위젯은 전 글에 같은 모양으로 붙어서
어느 글이 중요한지 구분해주지 못한다. 본문 링크는 「이 글이 저 글을 실제로 참조한다」는 신호다.

📌 실측(2026-09-09): 글 213편 중 **168편(78%)이 다른 글 본문에서 한 번도 안 걸린다.**
**시황·경제뉴스는 0.00** — 한 편도 안 내보내고 있었다. 재료는 이미 있는데 빠뜨린 자리다.



- [ ] 본문 **800자 이상**, 표·리스트로 구조화 (thin content 금지)
- [ ] slug은 **핵심 키워드 3~4단어 이내** 영문 케밥케이스
- [ ] frontmatter `publishedDate` = 오늘
- [ ] 차트: 가로축 0 시작 · 캡션에 표본·기간·출처
- [ ] `npm run build` 통과

---

## 4. 발행 후

- [ ] **정정할 땐 본문에 정정 안내를 남긴다.** 조용히 숫자만 바꾸면 이미 읽은 사람이 모른다.
- [ ] 정정 안내에 **무엇을 왜 고쳤는지** 적는다. 정정문 자체가 같은 모호함을 반복하지 않게 한다.
- [ ] 글을 내릴 땐 git 이력에 남긴다(되돌릴 수 있게).

---

## 🛡️ 검수로 붙인 문장은 「왜」를 물건에 같이 적는다

**검수에서 추가된 안전장치는 겉으로 보면 그냥 한 줄이다.** 이유가 물건에 안 붙어 있으면
다음에 정리하는 사람이 **형식 요소로 보고 옮기거나 뺀다.** 몰라서가 아니라 **그 정보가 거기 없어서**다.

```
원고·문구 파일에 한 줄 같이 적는다
예)  06장 마지막 줄 = 가드 · 이유: 없으면 「A가 무조건 유리」로 읽힘 (복덕 9/5)
```

⚠️ **"옮기기 전에 낸 사람에게 묻는다"는 처방으로 약하다** — 그 사람이 자리에 없으면 깨진다.
**사람이 아니라 기록에 건다.**

> 실사고 2026-09-05 우주사주 카드: 명리 담당이 *"뿌리 없음=나쁨으로 읽히니 가드 한 줄 붙여달라"*
> 고 요청해 5번 항목이 들어갔다. 그 뒤 키리가 **구조가 어긋난다며 하단으로 내리자고 제안**했고,
> 제작자가 실행해 **가드가 빠진 카드가 컨펌 직전까지 갔다.** 명리 담당이 몇 분 안에 잡아 되돌렸다.
> 🔴 **가드를 붙인 사람보다 그걸 옮기라는 사람이 더 셌다** — 근거는 앞이 대고 직책은 뒤가 가졌다.

📌 **제거·이동을 제안할 땐 「이게 원래 무슨 일을 하고 있었나」를 먼저 적는다.**
정리·금지 제안은 **잃는 쪽이 조용해서** 검증을 안 받는다. 같은 날 키리가 두 번 밟았다 —
`요일 금지`(청약일 실용 정보를 지울 뻔) · `가드 이동`(오독 방지선을 약화시킬 뻔). **둘 다 남이 막았다.**

---

## 검수 요청 형식 (작성자 → 키리)

```
PR 번호 · 제목 · slug
소재 출처   뉴스 링크 + 발행 시각          ← 0번 통과 근거
실측 내용   표본·기간·집계 방식
자신 없는 곳 1~2개                         ← 필수
```

> **왜 "자신 없는 곳"이 필수인가**
> 검수자는 **제출된 것만** 본다. 밖을 보게 하려면 작성자가 지목해야 한다.
> 2026-08-30 복덕이 내부 링크 대조를 지목한 덕에 **틀린 인용 + 원글이 금지한 비교** 2건이 잡혔다.
> 적을지 말지를 그때그때 판단하면 **자신 있을 때 안 적게 되는데, 자신 있을 때가 제일 위험하다.**
